require 'rails_helper'

RSpec.feature "Homepage", type: :feature do
  pending "shows a random person's diversity report summary"
  pending "links to current user's account if logged in"
  pending "links to begin authentication flow if not logged in"
end
